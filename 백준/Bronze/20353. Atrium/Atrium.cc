#include <bits/stdc++.h>
using namespace std;
#define FASTIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

int main(){
    FASTIO
    
    double d; cin >> d;
    
    cout.precision(6);
    cout<<fixed<<sqrt(d)*4;
}